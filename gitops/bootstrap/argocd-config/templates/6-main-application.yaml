---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "0-main"
  namespace: openshift-gitops
  finalizers:
   - resources-finalizer.argocd.argoproj.io
  annotations:
    # argocd.argoproj.io/sync-wave: "6"
    # "helm.sh/hook": ""
    "helm.sh/hook-weight": "6"
    argocd.argoproj.io/sync-options: "SkipDryRunOnMissingResource=true"
  labels:
    app.kubernetes.io/part-of: "{{ .Values.resourceNamePrefix }}"
spec:
  project: default

  source:
    repoURL: "{{ .Values.git.repository.repoURL }}"
    targetRevision: "{{ .Values.git.repository.targetRevision }}"
    path: "{{ .Values.git.repository.appOfAppsPath }}"
    helm:
      valueFiles:
      - values.yaml
      - values.{{ .Values.clusterName }}.yaml

  # Destination cluster and namespace to deploy the application
  destination:
    # name: change-me
    name: in-cluster
    namespace: openshift-gitops
    
  # NO NEED TO CHANGE BELOW PARAMS

  # Sync policy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
    - Validate=false
    - CreateNamespace=false
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    - RespectIgnoreDifferences=true
    
    retry:
      limit: 5
      backoff:
        duration: 15s
        factor: 1
        maxDuration: 900s


  # Apply metadata to namespaces
  # managedNamespaceMetadata:
  #   labels:
  #     argocd.argoproj.io/managed-by: openshift-gitops
  #   annotations:
  #     argocd.argoproj.io/sync-wave: '0'

  # ignoreDifferences:
  # - group: oadp.openshift.io
  #   kind: DataProtectionApplication
  #   jqPathExpressions:
  #     - '.spec.backupLocations'
  #     - '.spec.snapshotLocations'
  #     - '.spec.configuration.featureFlags'
  #     - '.spec.configuration.nodeAgent'

  revisionHistoryLimit: 10