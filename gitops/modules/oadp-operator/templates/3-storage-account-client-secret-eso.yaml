---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.storage.clientSecretKVSecretName  | lower }}
  namespace: {{ .Values.namespace }}
  annotations:
    managed-by: argocd.argoproj.io
    # argocd.argoproj.io/sync-wave: "3"
    # "helm.sh/hook": ""
    "helm.sh/hook-weight": "3"
    argocd.argoproj.io/sync-options: "SkipDryRunOnMissingResource=true"
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: {{ .Values.keyVaultName }}
  data:
    - secretKey: client_secret
      remoteRef:
        key: secret/{{ .Values.storage.clientSecretKVSecretName }}
  target:
    name: {{ .Values.storage.clientSecretKVSecretName | lower }}
    creationPolicy: "Owner"
    template:
      # Possible Values: "Opaque", "kubernetes.io/dockerconfigjson", "kubernetes.io/tls", "kubernetes.io/ssh-auth"
      type: Opaque
      data:
        {{ .Values.veleroSecretKeyFile }}: |
          AZURE_CLOUD_NAME={{ .Values.storage.azureCloudName }}
          AZURE_SUBSCRIPTION_ID={{ .Values.storage.subscriptionId }}
          AZURE_TENANT_ID={{ .Values.storage.tenantID }}
          AZURE_RESOURCE_GROUP={{ .Values.storage.resourceGroup }}
          AZURE_CLIENT_ID={{ .Values.storage.clientID }}
          AZURE_CLIENT_SECRET={{ `{{ .client_secret }}` }}