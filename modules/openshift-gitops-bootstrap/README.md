# openshift-gitops-bootstrap

Wrapper for the existing GitOps bootstrap implementation in `gitops/bootstrap/`.

This module accepts the rendered `stack` object plus the managed-cluster kubeconfig path.
